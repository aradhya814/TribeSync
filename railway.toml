[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx drizzle-kit push --force && npx tsx src/lib/db/seed.ts && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
